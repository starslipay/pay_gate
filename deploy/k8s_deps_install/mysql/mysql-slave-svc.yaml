apiVersion: v1
kind: Service
metadata:
  name: mysql-slave
  namespace: pay-ns
spec:
  type: ClusterIP
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
  selector:
    app: mysql
    mysql-role: slave