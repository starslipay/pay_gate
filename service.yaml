apiVersion: v1
kind: Service
metadata:
  name: pay-gate-service
  namespace: gateway-ns
  labels:
    app: pay-gate
spec:
  type: NodePort
  selector:
    app: pay-gate
  ports:
  - name: http
    port: 8888
    targetPort: 8888
    nodePort: 30888
    protocol: TCP
