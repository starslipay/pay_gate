apiVersion: v1
kind: Service
metadata:
  name: pay-gate-service
  namespace: pay-ns
  labels:
    app: pay-gate
spec:
  type: NodePort
  selector:
    app: pay-gate
  ports:
  - name: http
    port: 8888
    targetPort: 8888
    nodePort: 30880
    protocol: TCP
